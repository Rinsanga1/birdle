// valid words for both modes (shared)
const List<String> validWords = [];

// endless mode target words (you can update these separately)
const List<String> targetWords = validWords;

// target words for daily mode (you can update these separately)
const List<String> targetWordsDaily = validWords;
